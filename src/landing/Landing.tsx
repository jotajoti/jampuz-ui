import {Layout} from "../Layout";

export const Landing = () => {
    return (
        <Layout
            navigationStart={<a className="btn btn-ghost text-xl">Jampuz</a>}
        >
            Hello
        </Layout>
    );
}
